import React from "react";
import ReactDOM from "react-dom";
import App from "Components/App";
import "./typography";

ReactDOM.render(<App />, document.getElementById("root"));

//suspense api는 데이터가 있을 때까지 컴포넌트를 렌더링하지 않음!!
